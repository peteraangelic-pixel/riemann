name: Kaggriculture submit V16 Kanno TOP7 champion
on:
  push:
    branches: ["arena/01a0712c-riemann"]
    paths: [".github/workflows/kaggriculture-submit-v16-kanno.yml"]
permissions:
  contents: write
jobs:
  submit:
    if: contains(github.event.head_commit.message, '[kaggr-submit-v16-kanno]')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with: {python-version: "3.12"}
      - run: pip install --disable-pip-version-check kaggle
      - name: Validate and package promoted agent
        run: |
          python -m py_compile kaggriculture_meta_lab/agents/variants/agent_v16_kanno_top7_champion.py
          PYTHONPATH=kaggriculture_meta_lab python - <<'PY'
          from kaggriculture_lab.rust_backend import _source
          p="kaggriculture_meta_lab/agents/variants/agent_v16_kanno_top7_champion.py"
          tape=_source(p)
          assert tape is not None and tape.trim_hands
          assert len(tape.actions)==2 and all(len(stream)==719 for stream in tape.actions)
          PY
          work=$(mktemp -d)
          cp kaggriculture_meta_lab/agents/variants/agent_v16_kanno_top7_champion.py "$work/main.py"
          tar -C "$work" -czf "$work/submission-v16-kanno-top7.tar.gz" main.py
          test $(wc -c < "$work/submission-v16-kanno-top7.tar.gz") -lt 100000000
          echo "$work" > /tmp/workdir
      - name: Submit to Kaggle
        env: {KAGGLE_API_TOKEN: "${{ secrets.KAGGLE_API_TOKEN }}"}
        run: |
          mkdir -p ~/.kaggle
          printf '%s' "$KAGGLE_API_TOKEN" | tr -d '[:space:]' > ~/.kaggle/access_token
          chmod 600 ~/.kaggle/access_token
          work=$(cat /tmp/workdir)
          kaggle competitions submit -c kaggriculture \
            -f "$work/submission-v16-kanno-top7.tar.gz" \
            -m "V16 Kanno TOP7 best-sub champion 212-44 V15 16-0 G2 $GITHUB_SHA"
          sleep 15
          kaggle competitions submissions -c kaggriculture --csv > kaggriculture_meta_lab/results/kaggle-v16-kanno-status-20260910.csv
          python - <<'PY'
          import csv,json
          rows=list(csv.DictReader(open('kaggriculture_meta_lab/results/kaggle-v16-kanno-status-20260910.csv')))
          row=next(r for r in rows if 'V16 Kanno TOP7' in r['description'])
          json.dump(row,open('kaggriculture_meta_lab/results/kaggle-v16-kanno-submission-20260910.json','w'),indent=2)
          print(row)
          PY
      - name: Persist submission reference
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add kaggriculture_meta_lab/results/kaggle-v16-kanno-status-20260910.csv kaggriculture_meta_lab/results/kaggle-v16-kanno-submission-20260910.json
          git commit -m "Record V16 Kanno Kaggle submission"
          git push origin HEAD:arena/01a0712c-riemann
